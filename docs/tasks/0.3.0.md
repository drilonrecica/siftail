# Siftail `0.3.0` implementation tasks

**Status:** Planned
**Release state:** Unreleased
**Task IDs:** SFT-025–SFT-037
**Milestone authority:** `PRODUCT.md` §19.3

This milestone makes Siftail personally usable without pulling backup, export,
or other `0.4.0` hardening forward. Tasks remain subject to the authoritative
specifications and accepted tests.

---

### SFT-025 — Implement the bounded post-commit Live broker

**Status:** Done
**Milestone:** 0.3.0
**Depends on:** SFT-024
**Issue/PR:** — (direct maintainer implementation)
**Completed:** 2026-07-28
**Completion evidence:** The application lifecycle now owns one bounded Live
broker and drains the ingestion writer before stopping it. Committed batches
carry assigned event, source, and container-instance IDs and are sorted before
nonblocking publication. The broker caps command ingress at 256 commands and
the queued publication subset at 10,000 events/16 MiB; each of at most 16
subscribers has a 256-message/2 MiB ring. Subscriber or broker saturation closes
affected subscriptions with an explicit overflow outcome. Focused tests cover
ordering, exact filters, count/byte/connection limits, slow consumers,
publication loss, invalid filters, concurrent subscribe/publish/close, shutdown,
and publish-after-stop. Real HTTP/SQLite integration proves no pre-commit or
rolled-back publication. `go fmt ./...`, `go vet ./...`, `go test -count=1
./...`, `go test -race -count=1 ./...`, and `git diff --check` pass.

**Authoritative references:** `DOMAIN.md` §25; `ARCHITECTURE.md` §§14, 17;
`PRODUCT.md` §19.3.

**Outcome:** Add a lifecycle-owned broker that accepts only committed events
and fans them out without ever blocking the ingestion writer.

**Acceptance:**

- Subscribers receive newly committed events in assigned-ID order.
- Per-client delivery and broker command queues are bounded; slow clients are
  disconnected with an explicit overflow outcome.
- Publish remains nonblocking and never exposes uncommitted or rolled-back
  events.
- Shutdown closes subscriptions and owned goroutines deterministically.

**Verification:**

- Unit and race tests cover ordering, overflow, subscribe/unsubscribe races,
  slow consumers, shutdown, and publish-after-stop.
- Real ingestion integration proves commit-before-publication and no
  publication on rollback.

**Impact:** No migration or public route. Adds bounded resident memory and
concurrency owned by the application lifecycle; payloads remain absent from
process logs.

### SFT-026 — Add authenticated SSE transport and control events

**Status:** Done
**Milestone:** 0.3.0
**Depends on:** SFT-025
**Issue/PR:** — (direct maintainer implementation)
**Completed:** 2026-07-28
**Completion evidence:** Added the protected `GET /logs/live/stream` EventSource
transport with exact same-origin and Accept validation, bounded source/level/
stream/literal-text filters, no-store/no-buffer headers, five-second write
deadlines, and combined 8 KiB message/attribute previews. Streams revalidate
server-side sessions every five seconds and emit distinguishable heartbeat,
possible-gap, truncation, source purge/removal, session-invalid/unavailable, and
shutdown controls. Real streamed HTTP and race tests cover authentication,
filtering, hostile JSON content, reconnect boundaries, heartbeat, online
revocation, cancellation, connection saturation, slow-write overflow, source
controls, shutdown, security headers/content type, and absence of session,
query, or event payloads from process logs. `go fmt ./...`, `go vet ./...`, `go
test -count=1 ./...`, `go test -race -count=1 ./...`, focused repeated race
tests, and `git diff --check` pass.

**Authoritative references:** `DOMAIN.md` §§25, 27; `ARCHITECTURE.md` §§17–18,
21–22; `DESIGN.md` §§7–9.

**Outcome:** Expose the Live broker through an authenticated, filtered SSE
endpoint with bounded connections and explicit lifecycle control events.

**Acceptance:**

- Only valid administrator sessions may open streams; expired or revoked
  sessions close promptly.
- The endpoint applies supported source, level, stream, and text filters
  without inventing a second query language.
- Heartbeat, overflow, source purge, and server-shutdown controls are
  distinguishable from log events.
- Connection count, write deadlines, and response buffering are bounded; proxy
  buffering is disabled where required.

**Verification:**

- HTTP and race tests cover authentication, filters, reconnect boundaries,
  overflow, cancellation, shutdown, security headers, and content type.
- Sensitive log tests prove session tokens and event payloads are not logged.

**Impact:** Adds an authenticated browser route and long-lived reads. No
migration; bounded connection state and heartbeat traffic are documented.

### SFT-027 — Build the bounded Live workspace and client lifecycle

**Status:** Ready
**Milestone:** 0.3.0
**Depends on:** SFT-026
**Issue/PR:** —

**Authoritative references:** `PRODUCT.md` §§8.2, 19.3; `DESIGN.md` §§8–13,
32–39; `ARCHITECTURE.md` §§17–18.

**Outcome:** Add a Live mode separate from History with safe rendering,
pause/resume, reconnect behavior, and strict browser-side event caps.

**Acceptance:**

- History and Live remain separate modes and Live filter state is explicit.
- Pause, resume, clear-view, connection status, and manual reconnect behave as
  specified without mutating retained logs.
- The browser renders at most 1,000 rows and retains at most 2,000 pending
  events; overflow is visible and recoverable.
- Auto-scroll stops when the user moves away from the end, and reconnects never
  duplicate listeners or leak timers.
- Event content is inserted only with text-safe DOM APIs.

**Verification:**

- Handler/template and Playwright tests cover pause/resume, reconnect,
  overflow, clear-view semantics, focus, escaping, and session invalidation.
- Keyboard, dark/light, reduced-motion, 390px responsive, and axe smoke checks
  pass.

**Impact:** Adds embedded HTML/CSS/vanilla JavaScript only. Browser memory and
DOM growth are explicitly capped; no production dependency or telemetry.

### SFT-028 — Close Live concurrency, reconnection, and browser coverage

**Status:** Planned
**Milestone:** 0.3.0
**Depends on:** SFT-027
**Issue/PR:** —

**Authoritative references:** `ARCHITECTURE.md` §§17, 32–33; `DESIGN.md`
§§39–41; `PRODUCT.md` §19.3.

**Outcome:** Establish the release-level evidence that Live remains correct
under disconnects, slow clients, ingestion bursts, and browser navigation.

**Acceptance:**

- Concurrent publish/subscribe/disconnect stress completes without races,
  leaks, writer stalls, or unbounded memory.
- Reconnection and navigation preserve the documented History/Live boundary
  and do not replay unsupported history.
- Measured connection and buffer limits match the resource ledger.

**Verification:**

- Run targeted stress and leak tests, `go test -race ./...`, Playwright Live
  scenarios, and broker/SSE benchmarks.
- Record the fixture, commands, host, settings, results, and measured
  limitations in performance/release documentation.

**Impact:** Test and documentation work only unless evidence reveals defects.
No migration or expanded product scope.

### SFT-029 — Implement the source catalog and lifecycle

**Status:** Planned
**Milestone:** 0.3.0
**Depends on:** SFT-028
**Issue/PR:** —

**Authoritative references:** `DOMAIN.md` §§12–15, 30–31; `ARCHITECTURE.md`
§15; `DESIGN.md` §15.

**Outcome:** Provide authenticated source browsing with active/inactive
lifecycle state while preserving stable source identity.

**Acceptance:**

- The catalog includes inactive sources and exact Server-to-source hierarchy,
  last-seen state, aliases, and retained-event facts.
- Container instances remain observations, not stable source identity.
- Reads are bounded and ordered deterministically; incoming metadata cannot
  cross a Server boundary.

**Verification:**

- Real-SQLite store and handler tests cover hierarchy, inactivity, null
  metadata, pagination/bounds, escaping, and cancellation.
- Query plans and representative large-catalog behavior are reviewed.

**Impact:** No migration unless the implementation proves one necessary with
plan and benchmark evidence. Adds authenticated reads and source UI.

### SFT-030 — Add source aliases, clear-logs, and remove-source workflows

**Status:** Planned
**Milestone:** 0.3.0
**Depends on:** SFT-029
**Issue/PR:** —

**Authoritative references:** `DOMAIN.md` §§13, 17, 30–31; `DESIGN.md` §15;
`ARCHITECTURE.md` §§15, 18.

**Outcome:** Add safe source presentation and lifecycle mutations without
changing source identity or confusing browser-only clearing with deletion.

**Acceptance:**

- Alias changes affect presentation only and do not rewrite event ownership or
  source identity.
- Clear view remains browser-local; Clear logs deletes retained application
  events only after explicit confirmation.
- Remove source follows the documented ownership cascade and emits the required
  client control event without waiting for clients.
- All mutations require authentication, CSRF, exact Origin, and coordinator
  serialization.

**Verification:**

- Real-SQLite transaction tests cover rollback, ownership boundaries,
  concurrent ingestion, aliases, purge, and deletion.
- Playwright covers confirmations, copy, focus, session expiry, escaping, and
  destructive-action distinctions.

**Impact:** Destructive actions are explicit and audited once audit storage
arrives in `0.4.0`; no claim of forensic erasure. Migration need is determined
from the existing schema before implementation.

### SFT-031 — Add browser Server and ingestion-token management

**Status:** Planned
**Milestone:** 0.3.0
**Depends on:** SFT-030
**Issue/PR:** —

**Authoritative references:** `DOMAIN.md` §§20, 30; `ARCHITECTURE.md` §§20,
30; `DESIGN.md` §16.

**Outcome:** Let the administrator create and manage Servers and independently
revocable ingestion tokens through protected browser workflows.

**Acceptance:**

- Token plaintext is generated securely, displayed once, and never stored,
  redisplayed, logged, or placed in URLs.
- Revocation and rotation take effect immediately and stay bound to the
  selected Server.
- Browser and CLI operations use the same coordinator-owned domain behavior and
  enforce the single-owner model.
- All browser mutations enforce CSRF, Origin, form limits, no-store, and safe
  confirmation behavior.

**Verification:**

- Store/handler tests cover creation, one-time display, rotation, revocation,
  rollback, races, authorization, and secret-free logs/errors.
- Playwright covers the complete keyboard-accessible management flow and
  verifies secrets do not survive navigation.

**Impact:** Expands authenticated UI only, not a public administration API.
Security documentation records token lifecycle and exposure boundaries.

### SFT-032 — Add bounded retention settings and controls

**Status:** Planned
**Milestone:** 0.3.0
**Depends on:** SFT-031
**Issue/PR:** —

**Authoritative references:** `DOMAIN.md` §§16–17, 31; `ARCHITECTURE.md` §23;
`DESIGN.md` §19.

**Outcome:** Persist and expose validated global age and database-size
retention settings.

**Acceptance:**

- Age and size limits are independently configurable within documented safe
  bounds and whichever triggers first is authoritative.
- Settings mutations run through the coordinator and are atomic.
- The UI explains application-log scope, oldest-first behavior, and that
  deletion is not forensic erasure.
- Invalid, ambiguous, or unsafe values fail explicitly without changing prior
  settings.

**Verification:**

- Real-SQLite tests cover defaults, persistence, rollback, concurrent updates,
  boundary validation, and restart behavior.
- Handler and Playwright tests cover CSRF/Origin, validation copy, focus,
  no-store, and session expiry.

**Impact:** May require an immutable settings migration if existing storage
cannot represent the contract. Adds no background deletion until SFT-033.

### SFT-033 — Implement bounded age and database-size retention

**Status:** Planned
**Milestone:** 0.3.0
**Depends on:** SFT-032
**Issue/PR:** —

**Authoritative references:** `DOMAIN.md` §§16–17; `ARCHITECTURE.md` §§10, 23;
ADR 0002.

**Outcome:** Add lifecycle-owned retention that deletes the canonical oldest
events in bounded writer-coordinated transactions.

**Acceptance:**

- Retention enforces global age and database-size limits, deleting by
  `min(event_at_us, received_at_us), id` in bounded chunks.
- It never deletes authentication, configuration, session, or audit records
  and never runs routine full `VACUUM`.
- Ingestion remains bounded and reads remain available while retention works;
  cancellation and shutdown leave a valid database.
- Purges produce client control events only after commit.

**Verification:**

- Real-SQLite and race tests cover age/size triggers, equal timestamps,
  concurrent ingestion/reads, rollback, cancellation, restart, and shutdown.
- Benchmarks record chunk latency, writer interference, WAL/database behavior,
  and resource impact at representative sizes.

**Impact:** Permanently deletes retained application events according to
administrator policy. Operational docs must explain sizing, recovery limits,
and filesystem-dependent space reclamation.

### SFT-034 — Add status, readiness, and ingestion statistics

**Status:** Planned
**Milestone:** 0.3.0
**Depends on:** SFT-033
**Issue/PR:** —

**Authoritative references:** `DOMAIN.md` §§23, 27–29; `ARCHITECTURE.md`
§§26–27; `DESIGN.md` §18.

**Outcome:** Provide minimal health boundaries and an authenticated,
sanitized status view backed by bounded operational state.

**Acceptance:**

- Liveness reports process response without failing for transient busy state;
  readiness reflects migrations, writer availability, writability, shutdown,
  and critical degradation.
- The status page shows only sanitized database, queue, ingestion, retention,
  and version facts with clear timestamps.
- Counters and diagnostic history are bounded and never contain application
  messages, credentials, headers, hashes, or environment dumps.

**Verification:**

- Handler/lifecycle tests cover healthy, busy, unavailable, degraded, and
  shutdown transitions plus request IDs and panic recovery.
- Browser tests cover authentication, refresh, no-store, escaping,
  accessibility, themes, and mobile layout.

**Impact:** Adds minimal health responses and an authenticated page. Resident
statistics have fixed bounds and no telemetry or outbound reporting.

### SFT-035 — Pin Coolify and Fluent Bit compatibility

**Status:** Planned
**Milestone:** 0.3.0
**Depends on:** SFT-034
**Issue/PR:** —

**Authoritative references:** `ARCHITECTURE.md` §28; `PRODUCT.md` §§10, 19.3;
`DOMAIN.md` §15.

**Outcome:** Establish tested, date-stamped Coolify and Fluent Bit compatibility
fixtures, including prevention of Siftail self-ingestion.

**Acceptance:**

- Supported versions and payload/config variants are selected from official
  current documentation during implementation and recorded without implying
  broader support.
- Fixtures cover Coolify source hierarchy, generic Fluent Bit HTTP, retries,
  gzip/formats, and trusted Server binding.
- Generated collection rules exclude Siftail's own container and tests detect
  recursive ingestion.

**Verification:**

- Run production-path integration and smoke suites against pinned fixtures,
  including retry, duplicate, restart, and exclusion scenarios.
- Record upstream references, versions, commands, results, and limitations.

**Impact:** Test fixtures and compatibility documentation; no automatic
outbound calls and no runtime dependency on Coolify or Fluent Bit.

### SFT-036 — Add generated configuration and guided test ingestion

**Status:** Planned
**Milestone:** 0.3.0
**Depends on:** SFT-035
**Issue/PR:** —

**Authoritative references:** `ARCHITECTURE.md` §§20, 28, 30; `DESIGN.md`
§§16–17; `PRODUCT.md` §19.3.

**Outcome:** Generate copy-safe Coolify/Fluent Bit configuration and guide the
administrator through a verifiable test event without exposing credentials.

**Acceptance:**

- Output is derived from an explicit Server/token and current public URL
  configuration, with safe quoting and self-ingestion exclusion.
- Secrets appear only in the immediate one-time workflow and are never
  persisted in browser history, diagnostics, or process logs.
- Guided testing distinguishes delivery, authentication, retryable failure,
  and committed receipt without claiming success before commit.
- Generic Fluent Bit instructions remain available and accurately scoped.

**Verification:**

- Unit and subprocess tests cover quoting, HTTPS/proxy variants, one-time
  secrets, committed test receipt, retries, and hostile names.
- Playwright covers copy, focus, responsive instructions, session expiry, and
  navigation away from one-time material.

**Impact:** Adds CLI/UI guidance but no public admin API or external runtime
service. Operator documentation gains the tested workflow.

### SFT-037 — Close the `0.3.0` personally usable gate

**Status:** Planned
**Milestone:** 0.3.0
**Depends on:** SFT-025, SFT-026, SFT-027, SFT-028, SFT-029, SFT-030, SFT-031,
SFT-032, SFT-033, SFT-034, SFT-035, SFT-036
**Issue/PR:** —

**Authoritative references:** `PRODUCT.md` §19.3; `ARCHITECTURE.md` §§32–33,
41; `DESIGN.md` §§40–41.

**Outcome:** Produce traceable evidence that every `0.3.0` requirement is
implemented and the personally usable target is safe to enter dogfooding.

**Acceptance:**

- A requirement-to-test matrix covers every `PRODUCT.md` §19.3 item.
- Live, lifecycle, tokens, retention, status, compatibility, and mobile
  workflows pass together through production paths.
- Security/privacy, resource, accessibility, and operational limitations are
  recorded; no release or tag is implied.

**Verification:**

- Run repository checks, full race tests, migration fixtures, smoke,
  Playwright/axe, relevant benchmarks, and `git diff --check`.
- Perform and record keyboard, dark/light, reduced-motion, responsive,
  escaping, Coolify, shutdown, saturation, and retention reviews.

**Impact:** Documentation and gate evidence only unless verification exposes
defects. Marks SFT-025–SFT-037 Done only after all required evidence passes.
